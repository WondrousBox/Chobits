{
  "id": "openai",
  "label": "OpenAI",
  "enabled": true,
  "icon": "providers/icons/openai.svg",
  "locales": {
    "en": { "label": "OpenAI", "fields": { "apiKey": "API Key", "baseUrl": "Base URL (optional)", "model": "Default model (e.g., gpt-4o-mini)" } },
    "zh-CN": { "label": "OpenAI", "fields": { "apiKey": "API Key", "baseUrl": "Base URL（可选，自定义网关）", "model": "默认模型（如 gpt-4o-mini）" } }
  },
  "fields": [
    { "key": "apiKey", "label": "API Key", "type": "password", "required": true },
    { "key": "baseUrl", "label": "Base URL (可选，自定义网关)", "type": "text" },
    { "key": "model", "label": "默认模型（如 gpt-4o-mini）", "type": "text" }
  ]
}
